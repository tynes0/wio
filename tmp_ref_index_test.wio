component Vec2 {
    x: i32;
    y: i32;
}

fn Bump(v: ref Vec2) {
    v.x += 1;
}

fn Entry() -> i32 {
    mut values: Vec2[] = [Vec2(1,2)];
    Bump(ref values[0usize]);
    return values[0usize].x;
}
