use std::console as Console;
use std::vector;
use std::io as io;

fn Entry() -> void
{
    mut f = std::io::Open("test.txt", std::io::OpenReadWriteCreate);
    if(std::io::IsOpen(f))
    {
        std::io::Write(ref f, "Hello, World!");
        std::io::Close(ref f);
    }
}