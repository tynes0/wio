use std::console as Console;
use std::algorithms as Algorithms;
use std::math as Math;

use test;

fn Add(x: i32, y: i32) -> i32
{
    return x + y;
}

fn MakeAdd() -> fn(i32, i32) -> i32
{
    return Add;
}

object Human
{

}


fn Entry() -> i32
{
    mut ent: Entity = Entity(12);
}