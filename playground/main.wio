use std::io as console;
fn MakeLabel<T, U, a1>(first: T, second: U, third: a1) -> string {
    return $"${first} | ${second} | ${third}";
}
fn Entry() -> i32 {
    console::Print(MakeLabel(1, 2.4, "Test"));
}