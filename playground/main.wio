use std::io as console;

object Box<T> 
{
    public value: T;

    public fn Get() -> T 
    {
        return self.value;
    }
}
fn Entry() -> i32
{
    let numberBox = Box(12);
    let textBox = Box("wio");
    console::Print($"Generic ctor deduction: ${numberBox.Get()}, ${textBox.Get()}");
}