realm std {
    use @CppHeader("std_vector.h");

    @Native
    @CppName(wio::runtime::std_vector::Vec2)
    component Vector2 {
        x: f64;
        y: f64;
    }

    @Native
    @CppName(wio::runtime::std_vector::PrintVec)
    fn PrintVec2(vec: Vector2);
}