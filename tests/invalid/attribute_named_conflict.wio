attribute cached() for component retain compile;

[attribute::Targets(component)]
[attribute::Conflicts(cached)]
attribute live();

[live, cached]
component InvalidCache {
    value: i32;
}

fn Entry() -> i32 { return 0; }
