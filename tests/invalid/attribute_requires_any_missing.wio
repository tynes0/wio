attribute cached() for component retain compile;
attribute live() for component retain compile;

[attribute::Targets(component)]
[attribute::RequiresAny(cached, live)]
attribute query();

[query]
component MissingAlternative {
    value: i32;
}

fn Entry() -> i32 { return 0; }
