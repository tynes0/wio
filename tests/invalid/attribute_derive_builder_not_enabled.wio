use std::attribute as attr;

[From(attr::DeriveProcessor<i32>)]
object PendingDerive {}

[attribute::Targets(component)]
[attribute::Processor(PendingDerive)]
attribute Derived();

[Derived]
component Value { number: i32; }

fn Entry() -> i32 { return 0; }
