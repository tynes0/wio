attribute json(component)()
    with attribute::conflict("serialization");
attribute binary(component)()
    with attribute::conflict("serialization");

component Record with json, binary {
    value: string;
}

fn Entry() -> i32 { return 0; }
