import "wio.io"

func f()
{
	for(var i = 0; i < 10; ++i)
	{
		Println(i);
		if(i == 5)
		{
			return i;
		}
	}
}

Println(f());

#* 
array arr = [2, [1,2,3], "String", 'C'];

arr.Append("Test");

var front = arr.Front();

front = "Merhaba";

Println(arr);

Println(arr.At(1).Length());

*#