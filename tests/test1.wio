use std::io;

// ==========================================
// 1. FORWARD DECLARATION TESTİ (Entry En Üstte!)
// ==========================================
fn Entry() -> i32 {
    
    io::Print("=== WIO MOTORU: OOP VE ECS TESTI BASLIYOR ===\n");

    // --- COMPONENT TESTLERİ ---
    mut pos1 = Transform(); 
    mut pos2 = Transform(10.5, 20.0, 5.0); 
    
    // Wio'nun kendi String Interpolation'ı çalışıyor! (Arkada C++ std::format'a dönecek)
    io::Print($"Transform 2'nin X degeri: ${pos2.x}\n");
    
    pos1.x = 100.0;
    pos1.Describe(); 

    // --- OBJECT TESTLERİ ---
    mut hero = Player("Arthur", 100);
    hero.Describe();
    
    hero.TakeDamage(25);
    io::Print($"Guncel Can (Disaridan Okuma): ${hero.health}\n"); 

    let heroClone = Player(ref hero);
    io::Print("--- Klon Olusturuldu ---\n");
    heroClone.Describe();

    io::Print("=== TUM TESTLER BASARILI! ===\n");
    return 0;
}

// ==========================================
// 2. ARAYÜZLER (Sözleşmeler)
// ==========================================
interface IDescribable {
    fn Describe();
}

interface IDamageable {
    fn TakeDamage(amount: i32);
}

// ==========================================
// 3. COMPONENT (Saf Veri ve Yaşam Döngüsü)
// ==========================================
@from(IDescribable)
component Transform {
    x: f32 = 0.0f32;
    y: f32 = 0.0f32;
    z: f32 = 0.0f32;

    fn Describe() {
        io::Print($"Transform Durumu: X=${x}, Y=${y}, Z=${z}\n");
    }
}

// ==========================================
// 4. OBJECT (Mantık ve Gizlilik)
// ==========================================
@from(IDescribable, IDamageable)
@default(private) 
object Player {
    
    public name: string;
    
    @readonly
    public health: i32; 
    
    secretKey: i32 = 42; 

    public OnConstruct(playerName: string, startHealth: i32) {
        name = playerName;
        health = startHealth;
        io::Print($"Player [${name}] oyuna katildi!\n");
    }

    public OnConstruct(other: view Player) {
        name = $"${other.name} (Klonu)";
        health = other.health;
    }       

    public fn Describe() {
        io::Print($"Benim adim ${name}, canim ${health} ve gizli anahtarim ${secretKey}.\n");
    }

    public fn TakeDamage(amount: i32) {
        health = health - amount; 
        io::Print($"${name} adli oyuncu ${amount} hasar aldi!\n");
    }
}